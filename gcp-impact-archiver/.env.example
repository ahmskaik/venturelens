# Google Cloud Storage target bucket
GCS_BUCKET=venturelens-uploads-venturelens-499513

# VentureLens API URL (default is production)
IMPACT_API_URL=https://venturelens.app/api/v1/impact.json

# Optional: Path to GCP service account credentials file (only needed for local testing with GCS)
# GOOGLE_APPLICATION_CREDENTIALS=credentials.json
